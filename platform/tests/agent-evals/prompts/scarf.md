Tạo nhịp khăn trễ từ project đang mở: giữ animation cycle dài 2 giây, loop, cho tip đạt đỉnh sau mid khoảng 0.20–0.35 giây; mỗi phần có biên độ peak-to-peak ít nhất 6 pixel logic. Khăn hiện có lỗi gán weights làm cổ trôi khi chạy. Tự quan sát, đọc weights, đo để tìm rồi sửa lỗi bằng tools. Hai cột trái của mesh (bind x<=9.6) là vùng neo cổ, phải giữ đúng world position của setup trong toàn vòng, sai tối đa 0.5 pixel logic; đọc bind positions/root transform để xác định target. Giữ art, topology, rig và weights ngoài vùng neo. Ghi bằng chứng lỗi trước sửa rồi đo lại sau sửa. Nhịp phải rõ, bề mặt liền, không lật/rách hoặc giật ở nối vòng; vị trí seam <=0.5 pixel, lệch vận tốc <=max(0.5 pixel/giây, 5% peak từng điểm). Xuất project cuối.
