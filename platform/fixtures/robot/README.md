# Gate 1 robot

Run `python3 platform/fixtures/robot/prepare.py` from repository root to reproduce
art-input.zip and commands.json. The ZIP contains original T01 PNGs and a root only:
no rig, attachments or animation. Import it through Editor's package chooser, then
invoke native-recipe.mjs with the Browser host's actual discovered tools handle.
`commands.json` supplies explicit public command arguments, not hidden project edits.
The correction in docs/product/results/experiment-1/corrected-wave.json reduces
three drafted hand oscillations to the brief's two.

`native-project.zip` is the actual downloaded revision 5 from the native agent's
editor Session, after the observed correction. Open it directly in `/player.html`;
no Spine data, editor state or filesystem source paths are used by the player.
Source hashes, rights and placements remain in `../source/manifest.json` and
`../source/robot-layout.json`; these are internal evaluation assets.

See the locked brief and full evidence under docs/product/results/experiment-1.
This fixture's existence is not a claim that Gate 1 passed.
