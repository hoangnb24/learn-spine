# Initial run — retained, not Gate 2 acceptance

Source 370ddc1; protocol d836e8c, 2026-09-11. Actual command:
`GATE2_RUN=run-01 npx playwright test -c deformation.playwright.config.ts`.
Two tests passed (scarf/jelly); IK failed its Player seek timestamp expectation:
recorded time 1.9999999999999991 normalizes to 0 through the existing canonical
modulo expression, whereas the harness expected nearly 2. Saved poses/diagnostics
still pass arithmetic; the failed application check is not relabelled pass.

Visual inspection also found foot art rotating with shin, although endpoint
residual is only ~4.5e-14px. This fails the intended stable contact appearance.
Next fixture revision uses public putSlot to attach foot art to fixed target and
sets the ankle pivot. The independently measured IK endpoint/foot bone origin
remain required; no threshold or target changes. This original ZIP stays here.

Performance includes Playwright video recording and is an instrumented exploratory
run, not the final recording-free Gate1-config measurement. Subsequent run captures
only explicit actual-playback canvas videos, releases MediaRecorder tracks before
warmup, and includes observed loop values for every default diagnostic point.

`arithmetic-verification.json` was generated by verify.py; it does not overturn the
visual finding or failed seek test. Physics NOT TESTED. Full editor videos are in
history; independently recorded player videos are in player-videos.
